%h1 Brackets
- @tasks.each do |task|
  = link_to task_path(task) do
    .card
      .card_title
        = task.title
      .card_description
        = task.description
      %p= task.author_name
      .card_comments
        %p #{task.comments.count}💬
%p= link_to '+ Add new card', new_task_path
